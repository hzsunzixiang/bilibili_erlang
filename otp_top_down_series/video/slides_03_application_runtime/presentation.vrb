\frametitle{工具生成的工程骨架，已经把 OTP 入口放好了}
\begin{columns}[T,onlytextwidth]
    \column{0.54\textwidth}
    \begin{block}{目录结构}
\begin{lstlisting}[
  basicstyle=\ttfamily\tiny\color{terminalfg},
  backgroundcolor=\color{terminalbg},
  frame=single,
  rulecolor=\color{terminalborder},
  framesep=2.2mm,
  keepspaces=true,
  columns=fullflexible,
  escapeinside={(*@}{@*)}
]
[ericksun@ERICKSUN-MC1:full_otp_app] (main) $ tree
.
|-- README.txt
|-- (*@\textcolor{erlblue}{data}@*)
|-- rebar.config
`-- (*@\textcolor{erlblue}{src}@*)
    |-- data_cache.erl
    |-- data_store.erl
    |-- data_store_sup.erl
    |-- demo.erl
    |-- event_bus.erl
    |-- event_bus_handler.erl
    |-- (*@\textcolor{warning}{full_otp_app.app.src}@*)
    |-- (*@\textcolor{accent}{full_otp_app_app.erl}@*)
    |-- (*@\textcolor{accent}{full_otp_app_sup.erl}@*)
    |-- kv_server.erl
    `-- traffic_light.erl

3 directories, 13 files
\end{lstlisting}
    \end{block}
    \column{0.42\textwidth}
    \footnotesize
    \begin{block}{从目录读出三层入口}
      \begin{itemize}
        \item \code{full\_otp\_app.app.src}: 声明 application 是什么、依赖什么、从哪里启动。
        \item \code{full\_otp\_app\_app.erl}: application callback module，承接 \code{mod} 字段。
        \item \code{full\_otp\_app\_sup.erl}: top supervisor，启动后真正管理长期进程。
      \end{itemize}
    \end{block}
    \vspace{0.15cm}
    \begin{exampleblock}{连贯理解}
      目录不是孤立截图，它是在告诉我们：工具已经把 OTP application 的标准入口组织好了。
    \end{exampleblock}
  \end{columns}
